import { StyleSheet } from 'react-native';
import { RuiXian } from '../FontFamily';

export default StyleSheet.create({
  listTitle: {
    fontSize: 13,
    fontFamily: RuiXian,
    lineHeight: 15,
  },
});
